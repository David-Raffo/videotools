services:
  videotools:
    build: .
    image: videotools:latest
    container_name: videotools
    restart: unless-stopped
    user: "${PUID:-1000}:${PGID:-1000}"
    ports:
      - "${BIND:-127.0.0.1}:${PORT:-8080}:8000"
    env_file: .env
    volumes:
      - ./data:/data
